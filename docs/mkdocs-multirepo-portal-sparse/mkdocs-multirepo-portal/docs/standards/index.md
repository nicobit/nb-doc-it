# Documentation Standards

Each source repository should provide:

- a `mkdocs.yml` file;
- a documentation directory, normally `docs/`;
- relative links that stay inside its own documentation set;
- unique `site_name` paths such as `applications/application-a`.

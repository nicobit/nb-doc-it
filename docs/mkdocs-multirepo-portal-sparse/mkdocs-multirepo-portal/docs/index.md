# Enterprise Documentation Portal

This portal aggregates documentation from multiple GitLab repositories.

The central repository owns the global navigation, theme, plugins, and publication pipeline. Each source repository owns its own documentation files and local `mkdocs.yml` navigation.

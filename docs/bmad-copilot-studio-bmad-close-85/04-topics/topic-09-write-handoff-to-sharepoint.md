# Topic 09 — Write Handoff to SharePoint

## Trigger phrases

- Save handoff to SharePoint
- Write output files
- Store BMAD handoff package

## Goal

Use a configured action to write Markdown files to SharePoint.

## Important

SharePoint can be used as knowledge directly. Writing generated Markdown files back to SharePoint usually requires a configured action, Power Automate flow, or custom connector.

## Target folder

`BMAD-Copilot-Studio/05-output/bmad-handoff/<initiative-id>/`

## Action contract

Use `07-actions/sharepoint-write-handoff-package-action-contract.md`.
